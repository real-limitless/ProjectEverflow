# Everflow toolkit — Python API

FastAPI starter with `/health` and OpenAPI at `/docs`.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

Preview on port **8000**.
