# Everflow toolkit — Desktop GUI

Electron shell with a static renderer. In Everflow sandboxes (Linux microVM), use
`npm run web` so Preview can iframe the renderer; full Electron GUI needs the Desktop webtop.

```bash
npm install
npm run web   # Preview-friendly
# npm start   # Native Electron window (when display available)
```
